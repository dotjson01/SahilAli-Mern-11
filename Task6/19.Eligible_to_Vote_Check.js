// Eligible to Vote
// Check if a person is eligible to vote based on their age (age ≥ 18).


let age = 20;

if (age >= 18) {
  console.log(`You are ${age} years old and eligible to vote.`);
} else {
  console.log(`You are ${age} years old and not eligible to vote yet.`);
}